# === Variables d'environnement Phase Test Frontend ===

NODE_ENV=test

# === API Proxy ===
# LOCAL: http://localhost:3006 (backend sur port 3006)
# GCP: http://api-test:3000 (réseau Docker interne du backend)
API_PROXY_TARGET=http://localhost:3006
NEXT_PUBLIC_API_URL=http://localhost:3006

# === Google Maps (optionnel pour tests) ===
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
